{{- if .Values.cloudflare.ssmKey }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.cloudflare.secretName | quote }}
  namespace: {{ .Values.cloudflare.namespace | quote }}
spec:
  refreshPolicy: Periodic
  refreshInterval: {{ .Values.cloudflare.refreshInterval | quote }}
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: {{ .Values.cloudflare.secretName | quote }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: {{ .Values.cloudflare.secretKey | quote }}
      remoteRef:
        key: {{ .Values.cloudflare.ssmKey | quote }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
{{- end }}
