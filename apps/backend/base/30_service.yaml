apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: gitops-demo-backend
